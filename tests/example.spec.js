// @ts-check
const { test, expect } = require('@playwright/test');

test('Playwright测试', async () => {
  // 简单测试以验证Playwright是否正常工作
  console.log('Playwright测试运行正常');
  expect(true).toBeTruthy();
}); 